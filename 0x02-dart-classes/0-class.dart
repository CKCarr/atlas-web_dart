/**0. Welcome.
 * Create a User class:
 * 
 * That contains a property called name
 * and a method called showName that returns a string representation of User
 * “Hello name” 
 */

/// A class representing a User
class User {
  /// property: the name of the User
  String name = '';

  /// method: returns a string representation of User
  String showName() => 'Hello ${name}';
}
